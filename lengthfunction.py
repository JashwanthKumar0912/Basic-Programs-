#Ask the user to enter their name.

#Use the Len() function to find the number of characters in their name.

#Display the length to the user.

Name = str(input("Enter your name: "))
Name = str(len(Name))
print("Your name has " + (Name) + " characters.")
